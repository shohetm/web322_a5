<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Set - <%= set.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    <header>
        <%- include('partials/navbar', { page: '' }) %>
    </header>
    <main class="container mx-auto p-4">
        <section class="bg-gray-200 p-8 text-center">
            <h1 class="text-5xl font-bold">Edit Set - <%= set.name %></h1>
        </section>
        <section class="bg-white p-8 shadow-md rounded">
            <form action="/lego/editSet" method="POST">
                <div class="mb-4">
                    <label for="set_num" class="block text-gray-700">Set Number</label>
                    <input type="text" id="set_num" name="set_num" value="<%= set.set_num %>" readonly class="border border-gray-300 p-2 w-full cursor-not-allowed">
                </div>
                <div class="mb-4">
                    <label for="name" class="block text-gray-700">Name</label>
                    <input type="text" id="name" name="name" value="<%= set.name %>" class="border border-gray-300 p-2 w-full">
                </div>
                <div class="mb-4">
                    <label for="year" class="block text-gray-700">Year</label>
                    <input type="text" id="year" name="year" value="<%= set.year %>" class="border border-gray-300 p-2 w-full">
                </div>
                <div class="mb-4">
                    <label for="num_parts" class="block text-gray-700">Number of Parts</label>
                    <input type="text" id="num_parts" name="num_parts" value="<%= set.num_parts %>" class="border border-gray-300 p-2 w-full">
                </div>
                <div class="mb-4">
                    <label for="theme_id" class="block text-gray-700">Theme</label>
                    <select id="theme_id" name="theme_id" class="border border-gray-300 p-2 w-full">
                        <% themes.forEach(theme => { %>
                            <option value="<%= theme.id %>" <%= (set.theme_id == theme.id) ? "selected" : "" %>><%= theme.name %></option>
                        <% }) %>
                    </select>
                </div>
                <div class="mb-4">
                    <label for="img_url" class="block text-gray-700">Image URL</label>
                    <input type="text" id="img_url" name="img_url" value="<%= set.img_url %>" class="border border-gray-300 p-2 w-full">
                </div>
                <div class="flex justify-between">
                    <button type="submit" class="bg-blue-500 text-white py-2 px-4 rounded">Update Set</button>
                    <a href="/lego/deleteSet/<%= set.set_num %>" class="bg-red-500 text-white py-2 px-4 rounded">Delete Set</a>
                </div>
            </form>
        </section>
    </main>
</body>
</html>
