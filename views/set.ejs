<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= set.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    <header>
        <%- include('partials/navbar', { page: '/lego/sets' }) %>
    </header>
    <main class="container mx-auto p-4">
        <section class="bg-gray-200 p-8 text-center">
            <h1 class="text-5xl font-bold"><%= set.name %></h1>
        </section>
        <section class="grid grid-cols-1 md:grid-cols-2 gap-6 py-6">
            <div class="bg-white shadow-xl rounded p-4">
                <figure><img src="<%= set.img_url %>" alt="<%= set.name %>" class="w-full rounded"></figure>
            </div>
            <div class="bg-white shadow-xl rounded p-4">
                <h2 class="text-xl font-bold">Set Details</h2>
                <p><strong>Theme:</strong> <a class="bg-blue-500 text-white py-2 px-4 rounded" href="/lego/sets?theme=<%= set.Theme.name %>"><%= set.Theme.name %></a></p>
                <p><strong>Year:</strong> <%= set.year %></p>
                <p><strong>Number of Parts:</strong> <%= set.num_parts %></p>
                <div class="mt-4 text-right">
                    <a class="bg-blue-500 text-white py-2 px-4 rounded" href="/lego/sets">Back to Sets</a>
                </div>
            </div>
        </section>
    </main>
</body>
</html>
